# 版本对应报告里实际跑通的那套环境（Python 3.12 + CUDA 12.1）
#
# PyTorch 要装 CUDA 版，不能直接 pip install torch，否则拿到 CPU 版跑不动 GPU：
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#
# 然后 pip install -r requirements.txt 装其余部分。

ultralytics==8.4.48
numpy==2.1.3
opencv-python==4.13.0.88
matplotlib==3.11.0
scikit-learn==1.5.2
PyYAML==6.0.2
Pillow==11.0.0
